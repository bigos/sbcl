* Notes

In this method:
file:./src/pcl/init.lisp::133

we have
[[file:./src/pcl/init.lisp::when (and typep (not t))]]

That needs further investigation to see if we can have type checking at the make-
instance stage.
